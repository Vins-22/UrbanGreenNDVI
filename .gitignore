# Ignore all .tif files and related auxiliary files
*.tif
*.tif.*
*.ovr
*.tfw
*.aux.xml

# Don't ignore .zip files (we want to keep them)
!*.zip

# Ignore everything inside data/ folder
data/*

# But don't ignore zip files inside data/
!data/*.zip

# Standard ignores
__pycache__/
*.pyc
*.pyo
*.pyd
.DS_Store
.env
*.env
*.log
.ipynb_checkpoints/
.vscode/
